
function sayHello(name){
    console.log('Hello ' + name);
}

// sayHello('Taylor')
  console.log(module)

// target the folder the file is in to load the module; in this example logger.js is in the same folder
 var logger = require('./logger');

logger.log('message');